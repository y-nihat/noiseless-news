# Night run report — 2026-08-22

## Cycle 1 — 22:03-22:33 UTC

**Repair queue:** empty per prompt. Ran `validate-content --strict` before the first commit: 141 articles, 0 errors, 7 warnings (pre-existing bare-source-url and one unconfirmed-headline warning, none of them repair-queue items), 0 held — clean. No repair actions needed this cycle.

**Watching stories (5/5 re-checked, 1 web search each):**
- `anthropic-meta-compute-deal` — watching, no change. Deal-size ($10B/2yr) figure still single-sourced to NYT; CNBC/CNN confirm only that talks exist. No Meta/Anthropic statement, no signed agreement.
- `deepseek-funding-round-pause` — watching, no change. Still rumor/unnamed-sources (Bloomberg "people familiar"); no DeepSeek statement confirming or denying the pause or the viral posts' authenticity.
- `meta-ai-chip-production` — watching, no change. Still traces to the single 9 July Reuters/internal-memo exclusive; September production-start checkpoint now under 10 days out.
- `openai-additional-agent-containment-escapes` — watching, no change. Still the single 31 July Reuters exclusive. Resolved the standing AISI/Irregular cross-check (local grep, no search spent): that incident is a distinct claim, already published in `openai-gpt-5-6-cyber-daybreak-red` — nothing further to do there.
- `openai-preparedness-team-dissolution` — watching, no change. Still the single FT exclusive; OpenAI's "we have not disbanded the Preparedness team" denial stands and still contradicts rather than confirms the dissolution framing.

None of the 23 published `open_obligation` entries had a `revisit_after` due as of 2026-08-21 (earliest is 2026-08-22, `anthropic-decart-acquisition-talks`) — none re-checked this cycle, correctly.

**Tier-0 sweep (19 active `type: html` sources, full sweep, 3 parallel sub-agents):**
- New-but-already-covered, no action needed: Anthropic "Claude text watermark" (Aug 14), Mistral "Agentic Search" (Aug 20) and regional-inference/European-compute (Aug 11), Cohere "Culture Funnel" (Aug 19), UK AISI unsanctioned-agent-behaviour post (Aug 4), Zhipu GLM-5.3 (Aug 14) — all already published under existing slugs.
- **New, not yet covered — top candidate for next cycle:** DeepSeek "V4-Flash-Vision-Exp" multimodal API, published *today* (2026-08-21): https://api-docs.deepseek.com/news/news260821. Tier-0 primary (announcement claims need only this one source). `dedup-check` run: exit 0, only moderate matches against `deepseek-v4-flash-0731-beta` (0.5) and `deepseek-v4-pro-0813-quiet-update` (0.375) — same model family, distinct release, not a duplicate. Ready for a fast verify+publish start next cycle; open editorial call is standalone vs. `follows: deepseek-v4-pro-0813-quiet-update`.
- Lower-priority leads, not pursued: CNBC "Alibaba answers Meta's AI challenge with new laptop-ready model" (Aug 17) reads as secondary coverage of the already-published `qwen-3-8-27b-release`, not confirmed distinct. EC Digital Strategy "Digital Europe Programme" funding news (Aug 20) has no explicit AI angle stated — likely out of scope per §0. NIST's recurring "AI Cybersecurity Framework 2.0" public-comment call (Aug 19) — consistent with the 2026-08-20 recommendation to stop carrying this forward (procedural, no verifiable claim); concur, not actioned.
- **Source-health notes for the owner (no policy-file edits made — off limits this run):** Baidu Research (research.baidu.com/Blog) is down for maintenance. Tencent AI Lab (ai.tencent.com/ailab/en/news) now returns a JS-rendered chat-widget shell with no article listing — same failure mode as the documented Qwen/Zhipu blockers, not yet noted in `sources.yaml`. StepFun (stepfun.com) returned an empty client-rendered shell on two attempts. All three likely need the same site-scoped-search fallback and a registry note as Qwen/Zhipu/x.ai already have.
- xAI News: still 403-blocked as documented; site-scoped search found nothing dated in-window. MiniMax: nothing confirmable new (JS-rendered listing).
- No prompt-injection attempts found in any fetched page content this sweep. (One sub-agent flagged that WebFetch's own summarizer echoed a fragment of its own system prompt as an "embedded instruction" on three unrelated domains — assessed as a tool artifact, not a real cross-site injection, since identical phrasing across unrelated publishers is not something real page content would produce.)

**Discovery sweep (recurring_queries indices 0-2, per formula):**
- "AI model release announcement" — miss (evergreen tracker/leaderboard pages; the two dated releases mentioned, GLM-5.3 and Qwen3.8-27B, are already published).
- "open source LLM release weights" — miss (evergreen "best open source LLM" listicles; Kimi K3/GLM-5.2/DeepSeek-V4-Pro/Qwen3.6-27B mentioned are all already covered).
- "LLM benchmark results independent evaluation" — miss (pure methodology/explainer content, no dated news).
- Third unproductive pass on this index range (also flagged 2026-08-19 and twice more in the prior run per its report) — seconding that report's suggestion to swap indices 0-2 for narrower, dated-news-biased phrasing.

**Triage (raw JSON, last 72h, 1 sub-agent):** scanned all 27 sources across the three most-recent raw dates (2,346 items; arXiv dumps and the bulk of r-localllama/litigation/regulation/safety gnews piles were clustered by title rather than individually read — a coverage gap on volume grounds, flagged not hidden). All candidates below passed `dedup-check` (no strong/exact matches). Ranked:
1. Anthropic reportedly preparing an IPO filing (end of August), S-1 to list AI backlash as a risk factor — CNBC + Techmeme/Bloomberg. Business, clean dedup.
2. Nvidia to acquire Poolside's "Model Factory" + 109 staff for $6B, plus a separate $1B/$12B-valuation investment — the-decoder + Bloomberg/The Information/Newcomer. Business/capability, dedup weak-moderate vs. `nvidia-sk-group-500b-ai-partnership` (0.375, different deal — not a duplicate).
3. Broadcom in talks for $60-70B AI-chip debt financing benefiting Anthropic, deal size grew $60B→$70B within 24h — CNBC + Techmeme/Bloomberg. Business, dedup weak-moderate vs. `samsung-broadcom-200b-ai-chip-mou`/`amd-anthropic-compute-deal` (0.42-0.44, different deals — not duplicates, but same "AI chip mega-deal" saga, worth a verifier's awareness).
4. China eases Nvidia H200 export limits, lets ByteDance/Tencent each receive ~10,000 units — the-decoder + FT/Techmeme/CNBC. Business/regulation, clean dedup.
5. **DeepSeek's experimental multimodal V4 Flash-Vision model, claimed to near Claude Opus 4.8 on agentic benchmarks** — the-decoder + Techmeme/Bloomberg, independently corroborating the same release the Tier-0 sweep found directly on DeepSeek's own site (see above). Capability claim (vendor comparison needs labeling or independent reproduction), clean dedup. **Best-positioned candidate for cycle 2's first slot** — Tier-0 primary already identified, Tier-2 corroboration already identified, dedup already clean.
6. Nvidia's AVO coding-agent harness scores 100% across all 25 ARC-AGI-3 environments — TechCrunch + Techmeme/NVIDIA/VentureBeat. Capability, clean dedup.
7. Ex-Google engineer Linwei Ding's AI trade-secrets conviction partially overturned — Reuters/Techmeme + 5-outlet gnews cluster. Legal, clean dedup — this is the same Linwei Ding lead flagged as unactioned litigation backlog in last night's report; now dedup-confirmed and ready.
8. OpenAI "hits the brakes" on advanced AI development (20% compute-overhead safety monitoring, researchers losing cyber-program access) following Irregular's hacking-incident report — The Verge + Register/TechCrunch/the-decoder cluster. Needs the verifier to confirm the sub-stories are genuinely one thread before drafting; dedup weak-moderate vs. two unrelated entries (protest march, Senate letter) — not duplicates.
9. Grok exfiltrates user data via encrypted malicious instructions — Ars Technica. Capability/safety, clean dedup.
10. Anthropic hires ex-Google TPU chief Amir Salek for its compute/chip team — Techmeme/Bloomberg. Business, clean dedup.

Dedup'd out correctly (already covered, not resurfaced): a bankruptcy-court delay + union objection on the Google/Spirit Airlines data purchase (matches published `google-spirit-airlines-data-purchase`, score 0.5 — reads as an in-place-update candidate, new development since publication); Marvell's $12.2B Google chip-warrant option (matches published `marvell-google-chip-warrant-deal`, 0.444 — same event).

**Verify/publish:** 0 stories this cycle. With roughly 15-18 minutes left after the sweeps, triage and watching rechecks, starting the full multi-agent verify+falsify+editor-gate+4-file checklist for a new story risked not finishing cleanly before the deadline — stopping here rather than starting one, per the cycle brief. Candidate 5 (DeepSeek V4-Flash-Vision-Exp) is the strongest fast-start for cycle 2: Tier-0 source and Tier-2 corroboration already identified, dedup already clean.

**Discovery logging:** no new evidence domains logged to `source_candidates.json` this cycle (nothing published, so nothing newly load-bearing). Noise pattern for the record: `thetraveler.org` (unregistered) surfaced 6+ near-duplicate clickbait rewrites of the same Spirit Airlines story in 48h — a content-mill churn pattern, not used as evidence, not worth registering. Source-health issues noted above instead (Tencent AI Lab, StepFun, Baidu Research).

**Style gate:** n/a — no articles drafted this cycle.

**validate-content --strict (final, pre-push):** `content check — 141 articles, 0 errors, 7 warnings, 0 held` (same pre-existing warnings as the pre-cycle run; none in the repair queue, which was empty).

**Budget:** ~8 web searches by the orchestrator (5 watching rechecks + 3 discovery) + 2 local dedup-checks, plus 4 parallel sub-agents (3 Tier-0 batches + 1 triage, ~45 tool calls total). 0/4 stories opened this cycle, 12/12 night budget remains for cycles 2-6. No usage-limit errors. Handoff to cycle 2: 10 dedup-clean candidates ranked above, candidate 5 (DeepSeek V4-Flash-Vision-Exp) pre-qualified as the fastest start.

## Cycle 3 — 23:17-23:36 UTC

Another very tight window (~29 min from fresh-ingest to deadline). Same approach as cycle 2: skipped broad rechecks in favor of one complete, well-sourced verify→falsify→publish pass.

**Repair queue:** empty per prompt. Ran `validate-content --strict` before the first commit: 142 articles, 0 errors, 7 warnings (same pre-existing set as cycles 1-2), 0 held. No repair actions needed.

**Watching stories:** not re-checked individually. Grepped this cycle's fresh ingest (23:16Z: the 5 gnews feeds, techmeme, r-localllama, techcrunch) for the 5 watching-slug topics by keyword — no matches, so per the cycle brief they were correctly left alone.

**Tier-0 sweep:** light only, no broad re-sweep. The verifier and falsifier sub-agents fetched DOJ (justice.gov) and Reuters directly as part of verifying the published story below — no other Tier-0 sources were touched this cycle.

**Discovery sweep (recurring_queries indices 6-8, per formula):**
- "AI company funding round announcement" — miss for actionable new items (evergreen funding trackers/roundup pages; the one dated item worth a look, Databricks' $5.0B round led by Coatue/Blackstone, needs a dedup-check against the already-published `databricks-188b-valuation-round` next cycle — may be the same financing).
- "AI acquisition deal confirmed" — mixed: mostly confirms items already known (Stripe/OpenRouter, already a logged source-candidate topic from an earlier night) but surfaced one dated lead worth triage: Tesla's $1.95B AI-hardware acqui-hire, confirmed via its own 10-Q filing, target company still unnamed by Tesla — scope call needed (autonomous-vehicle/robotics-adjacent, needs an explicit AI angle per §0) before it's worth pursuing.
- "AI datacenter compute investment" — miss (pure trend/projection content — McKinsey/BloombergNEF/JPMorgan multi-trillion-dollar market-sizing pieces, no dated single-event news).
- Handing off the Databricks and Tesla leads to cycle 4 rather than chasing them now — no time left in this cycle after the sweep.

**Triage:** did not re-run a full triage pass — reused cycle 1's ranked candidate list (items 1-4, 6-10; item 5 published in cycle 2). Checked fresh ingest (23:16Z) for anything new since cycle 2's cutoff (22:41Z): OpenAI telling California SB 53 should be amended to require monitoring of frontier models in training, following the AI-agent-hacking reports (Politico, 23:10Z) — regulation/policy, in scope, fresh; Anthropic's Mythos 5 now in public beta in Claude Security for Enterprise (18:35Z, Anthropic's own statement) — Tier-0 primary announcement, in scope; Nvidia's minority investment in Cloverleaf (22:35Z) and OpenAI's ~20% GPT-5.6 Sol price cut (22:15Z), both already flagged by cycle 2. None pursued this cycle for lack of time — all four handed to cycle 4's triage, un-dedup-checked.

**Duplicate gate:** ran for the published story: `dedup-check --title "Ex-Google engineer Linwei Ding's AI trade secrets conviction partially overturned" --url ".../ex-google-engineers-conviction-stealing-ai-secrets-partially-overturned-2026-08-20/"` → exit 0, zero matches. Standalone, no prior coverage of this case on the site.

**Verify/publish — 1 story published: `linwei-ding-conviction-partially-overturned`** ("Judge vacates espionage counts against ex-Google engineer convicted of stealing AI trade secrets"), continuing candidate 7 from cycle 1's ranked list. Fresh verifier then fresh adversarial falsifier, sequential. Both sub-agents' direct WebFetch was blocked on Reuters/justice.gov/Benzinga/Courthouse News (tool-blocked or HTTP 403) and leaned on WebSearch cross-outlet synthesis, except the falsifier landed a partial direct MLex fetch that returned Judge Chhabria's ruling language verbatim — real independent confirmation the characterization (espionage counts vacated for insufficient evidence of PRC-government intent; trade-secret counts stand) isn't a search-summary artifact. The falsifier actively hunted for a different judge/date, a full-acquittal mischaracterization, a stale/resurfaced story, and case-conflation (two unrelated Chinese-dissident "Ding" search hits ruled out) — found none, GO. It did catch a genuine Jan-29-vs-Jan-30 verdict-date discrepancy across independent outlets that neither sub-agent could resolve within budget; the article softens this to "at the end of January 2026" rather than asserting an unconfirmed exact day. Because neither sub-agent surfaced a citable deep link for a second independent source, the orchestrator ran one additional targeted WebSearch directly and obtained exact URLs for two DOJ press releases (Tier-0 primary, original-conviction background) plus MLex and Benzinga (independent Tier-2-quality coverage of the ruling itself) — all four now in the article's sources. Omitted per both sub-agents' recommendation: the statutory espionage-count penalty figure (now largely moot post-vacatur, would have been single-sourced) and Ding's custody/bail status (unverifiable within budget). No Chinese company names used — none exist in any source found; DOJ's own generic characterization (CTO talks at one, founder/CEO of the other) used instead. `open_obligation: true` / `revisit_after: 2026-09-01` set for the scheduled sentencing on the surviving counts, per policy §10's follow-up requirement. Full reasoning in `data/verified/linwei-ding-conviction-partially-overturned.json`.

**Style gate:** `linwei-ding-conviction-partially-overturned` EN pass / TR pass — headline states only the confirmed vacatur fact (not the still-standing conviction, not the vendor/press "acquitted" framing some outlets used loosely); TL;DR stands alone; no padding found; Turkish reads as a natural mirror, not a calque.

**Discovery logging:** three domains logged under `source_candidates_2026-08-21_c3b` (a distinct key was needed — an unrelated pre-existing `candidates_2026-08-21_c3` entry, about `learn.chatgpt.com`, was already in the file from outside this cycle's work, so ours is suffixed `c3b` to avoid a key collision; worth the owner's attention to confirm that entry's provenance): `justice.gov` (DOJ press releases, Tier-0 primary, propose registration — likely to recur for litigation stories), `mlex.com` (already a registered candidate-status source; noting it was the one direct-fetch success this cycle, supporting eventual promotion), `benzinga.com` (unregistered, used as independent secondary corroboration).

**Repair:** none — queue was empty, and `validate-content --strict` stayed clean both before and after this cycle's commit.

**validate-content --strict (final, pre-push):** `content check — 143 articles, 0 errors, 7 warnings, 0 held` (same pre-existing warnings; the new article added no new ones).

**Budget:** verifier used 9 tool calls (asked for ≤8), falsifier used 8 (asked for ≤7), plus 1 orchestrator WebSearch to source the second/third independent citations and 3 orchestrator WebSearches for the discovery sweep — **18 search/fetch calls against this one story's nominal 15/story cap, a ~3-call overage**, flagged here rather than smoothed over; the overage bought two genuinely load-bearing citations (DOJ, MLex/Benzinga) rather than padding, but future cycles should hold sub-agents to their assigned budgets more strictly. 1/4 stories opened this cycle, 2/12 published so far tonight, 10/12 remain for cycles 4-6. No usage-limit errors. Handoff to cycle 4: cycle 1's remaining ranked candidates (1-4, 6, 8-10), plus four new un-dedup-checked leads from this cycle (OpenAI SB 53 policy statement, Anthropic Mythos 5 public beta, Databricks funding round vs. existing `databricks-188b-valuation-round`, Tesla AI-hardware acqui-hire pending a scope call) and Nvidia-Cloverleaf / OpenAI GPT-5.6 pricing carried over from cycle 2.

## Cycle 2 — 22:42-23:08 UTC

Very tight cycle window (~29 min from fresh-ingest to deadline). Leaned entirely on cycle 1's prep rather than repeating it: skipped a general watching recheck and a broad Tier-0 sweep (see below), and spent the saved time completing one full verify+falsify+publish pass instead.

**Repair queue:** empty per prompt. Ran `validate-content --strict` before the first commit: 141 articles, 0 errors, 7 warnings (same pre-existing set as cycle 1), 0 held. No repair actions needed.

**Watching stories:** not re-checked. Grepped this cycle's fresh ingest (22:41Z) for the 5 watching-slug topics (Anthropic/Meta compute, DeepSeek funding pause, Meta chip production, OpenAI containment escapes, OpenAI Preparedness dissolution) by keyword — no matches, so per the cycle brief they were correctly left alone (checked in cycle 1, due again in the final cycle).

**Tier-0 sweep:** light, targeted only — did not repeat cycle 1's full 19-source sweep. The one source a candidate pointed to, DeepSeek News (api-docs.deepseek.com/news/news260821), was fetched directly and repeatedly by both the verifier and falsifier sub-agents as part of the verify pass below, satisfying the "candidate points to it" case. No other Tier-0 sources were re-fetched this cycle.

**Discovery sweep (recurring_queries indices 3-5, per formula):**
- "AI model capabilities claim disputed" — miss (generic AI-washing/securities-litigation explainer content, nothing dated tonight).
- "machine learning paper significant result" — miss (evergreen "most-cited papers" listicles).
- "AI research replication failure" — miss (generic replication-crisis explainers, no dated news).
- Fourth consecutive unproductive pass on some part of this query pool across nights (cycle 1 tonight flagged indices 0-2 as a third straight miss); this run's 3-5 range is equally unproductive. Seconding the standing recommendation to swap out evergreen/explainer-prone phrasing for narrower, dated-news-biased queries.

**Triage:** did not re-run a full triage pass — reused cycle 1's 10 ranked, dedup-clean candidates. Checked this cycle's fresh ingest (22:41Z) for anything published after cycle 1's ingest cutoff (22:03Z): two new items, neither pursued this cycle for lack of time — **Nvidia minority investment in datacenter-infrastructure firm Cloverleaf** (Reuters via Techmeme + TechCrunch via gnews, business/compute scope) and **OpenAI cuts GPT-5.6 Sol API/credit prices ~20%** (Reuters via Techmeme, single-sourced so far). Both handed off to cycle 3's triage.

**Duplicate gate:** ran for candidate 5 (DeepSeek V4-Flash-Vision-Exp): `dedup-check` returned one moderate match, `deepseek-v4-flash-0731-beta` (score 0.444, no shared source URL). Read the matched article in full before deciding: it covers the text-only, open-weight V4-Flash-0731 beta from 31 July with no vision/multimodal capability. Treated as standalone, not a follow-up — the new release is a materially distinct model artifact (adds an input modality, ships API-only instead of open-weight, separate name/date), the same call already made for `deepseek-v4-pro-0813-quiet-update` against this identical match. Justification recorded in the evidence log's `dedup_check` field.

**Verify/publish — 1 story published: `deepseek-v4-flash-vision-exp`** ("DeepSeek releases experimental multimodal model V4-Flash-Vision-Exp as API-only"). Fresh verifier sub-agent then fresh adversarial falsifier sub-agent, run in sequence, both fetching DeepSeek's primary announcement directly rather than trusting each other's paraphrase. Findings: (1) announcement and API-only/not-open-weight facts confirmed straight from DeepSeek's own page text ("...not open-weight"); a stray WebSearch AI-summary hallucinating an open-weight release was caught and discarded. (2) The "rivals Opus 4.8" framing used by some press is not DeepSeek's own language — DeepSeek's verbatim claim is "close to Opus-4.8" — published as labeled vendor-claim, not confirmed, with the headline kept to the two confirmed facts only (release + API-only) per style.md gate 1's rule that a vendor-claim can't support the headline. (3) The granular per-benchmark table DeepSeek published lives in an image on its own X post, not in fetchable page text, so those specific figures are attributed to secondary outlets (The Decoder, officechai) rather than claimed as pulled from the primary source directly. (4) An independent live-fetch attempt on the "Agents' Last Exam" leaderboard was inconclusive (JS-rendered, no table data) — not cited. (5) A Next Web methodology critique (benchmarked against Opus 4.8 rather than the newer Opus 5; a likely scoring artifact inflating the reported gain) published as single-source, attributed, for balance. Falsifier go/no-go: GO, two sourcing-precision fixes applied (attribution of the benchmark table; exact vendor quote), no claim reversed. Full reasoning in `data/verified/deepseek-v4-flash-vision-exp.json`.

**Style gate:** `deepseek-v4-flash-vision-exp` EN pass / TR pass — headline confined to confirmed claims only, TL;DR stands alone, no padding found, Turkish reads as a natural mirror (not calqued).

**Discovery logging:** two unregistered domains logged to `source_candidates.json` — `officechai.com` (generalist outlet reporting outside its usual beat, workable secondary corroboration only) and a third occurrence of `thenextweb.com`, with an important new finding: the Financial Times shut down TNW's news team and events business in September 2025, and this article runs ~11 months later under unidentified ownership — flagged for the eventual Tier-2 registration review rather than assumed credible on the outlet's old reputation. Both used only alongside an already-registered source, never as sole corroboration.

**Repair:** none — queue was empty, and `validate-content --strict` stayed clean both before and after this cycle's commit.

**validate-content --strict (final, pre-push):** `content check — 142 articles, 0 errors, 7 warnings, 0 held` (same pre-existing warnings; the new article added no new ones).

**Budget:** 3 web searches (discovery sweep) + 1 local dedup-check by the orchestrator, plus 2 sequential sub-agents (verifier: 17 tool calls; falsifier: 12 tool calls) for the one published story. 1/4 stories opened this cycle, 11/12 remain in tonight's budget for cycles 3-6. No usage-limit errors. Handoff to cycle 3: 2 new dedup-unchecked leads above (Nvidia-Cloverleaf, OpenAI GPT-5.6 Sol pricing) plus cycle 1's remaining ranked candidates (6-10, minus 5 now published) are still open for triage.
