# Night run report — 2026-07-15 (started 2026-07-14 22:36Z)

## Cycle 1 — 22:36-22:49 UTC

Short window this cycle (deadline 23:09Z, ~30min nominal). Prioritized the
mandatory watching rechecks and one well-staged, fast-to-verify candidate
over a full Tier-0 sweep, given last night's report showed every rushed
cycle produces zero published stories.

**Watching rechecks (step 1):**
- `meta-ai-chip-production`: re-checked, 1 web search (site-scoped to
  exclude reuters.com). No change — all coverage still traces to the single
  9 July Reuters/internal-memo exclusive. Still watching.
- `openai-safety-heidecke-departure`: re-checked, 1 web search. No change —
  all coverage still relays the same Mark Chen quote given exclusively to
  Wired. Still watching. Reported effective-departure date (2026-07-24) is
  10 days out.

**Tier-0 sweep:** skipped this cycle for time — raw ingest (already fresh
from this cycle's automated pull) plus techmeme/gnews review covered the
same ground faster. Flagging for a later cycle tonight if window allows.

**Discovery sweep (step 3, queries at index 0-2):**
- "AI model release announcement" — miss (generic tracker/leaderboard
  pages, nothing new vs. already-ingested items).
- "open source LLM release weights" — miss (same, ranking/comparison
  content, no new candidate).
- "LLM benchmark results independent evaluation" — miss (benchmark
  explainer content, no new candidate).
Consistent with last night's report: this query pool is not finding
anything raw ingest + techmeme don't already surface. Recommend the owner
consider a rewrite (flagged again).

**Triage / candidates considered:**
- Anthropic research: "How Claude's values vary by model and language"
  (309,815 conversations, published 2026-07-13) — surfaced via techmeme.
  dedup-check: no match. Verified and **published** (see below).
- Demis Hassabis proposes a US-based "Frontier-class" AI standards body
  (personal manifesto, cross-posted to X) — dedup-check: no match. Verified
  and **published** (see below).
- DeepSeek preliminary fundraising talks at ~$71B valuation (FT) —
  single-sourced ("sources say", wire-exclusive risk) — skipped, not
  pursued this cycle.
- Nvidia cuts authorized AI chip customers in Singapore/Malaysia/Japan (FT
  "sources") — single-sourced — skipped.
- NY hyperscale-datacenter moratorium (Hochul) — likely out of scope per
  §0 (general datacenter/energy regulation, AI angle not established in the
  headline) — not triaged further.
- PixVerse Series C ($439M) — business claim, would need a second
  independent source; not pursued this cycle for time.

**Verification notes — published story:**
- `anthropic-claude-values-models-languages`: Tier-0 primary (Anthropic's
  own research post) directly fetched. Research-finding claim type — the
  post itself is the standard of proof (verification.md §3). Adversarial
  check: targeted web search for critique/dispute found only consistent
  secondary coverage (Gizmodo, officechai, Brave New Coin, cryptonomist);
  one outlet (Brave New Coin) flagged that the three studied models are
  now superseded by Anthropic's current lineup and the data is from May
  2026 — a temporal-scope caveat, reflected in the article's "why it
  matters" section, not a factual dispute. Not a duplicate of
  `anthropic-claude-reflect` (different Anthropic announcement, 9 July,
  about the Reflect usage-history feature).

- `hassabis-frontier-ai-standards-body`: primary text is Hassabis's own
  Substack post ("A Framework for Frontier AI and the Dawning of a New
  Age"), cross-posted to X — neither domain is registered in
  policy/sources.yaml. Substack returned HTTP 403 on direct WebFetch (same
  blocked-primary pattern as openai.com/theverge.com/x.ai); used site-scoped
  web search as fallback per that same precedent. Confirmed via three
  independent named-author Tier-2 outlets (Axios, TechCrunch, CNBC) that
  each read the public document directly — not a wire-exclusive relay
  chain, since the source document is public and each outlet's account
  carries independent framing. Cross-checked against The Decoder and
  TheNextWeb for consistency; no contradicting report found. Article frames
  this explicitly as a proposal, not adopted policy. Flagging
  demishassabis.substack.com for the owner to consider registering (used
  per verification.md §1, unregistered-but-qualifying-tier source) — not
  added to sources.yaml this cycle since policy files are off-limits this
  run.

**Style-gate notes:**
- style-gate: anthropic-claude-values-models-languages EN pass / TR pass
  (headline falsifiable, TL;DR standalone, no padding, Turkish mirrors
  structure natively — no calque phrasing).
- style-gate: hassabis-frontier-ai-standards-body EN pass / TR pass
  (headline states the falsifiable fact that he made the call, not the
  proposal's eventual adoption; TL;DR states what happened + notes it's a
  proposal; "Why it matters" explicitly flags proposal-vs-policy status
  instead of overstating it).

**Discovery logging:** two unregistered-but-Tier-qualifying evidence
domains used this cycle (Anthropic's own research subdomain is already
covered by the registered "Anthropic News" entry; demishassabis.substack.com
and x.com/demishassabis are genuinely new — flagged above for registration
review, not added to `source_candidates.json` since both are individual-
expert primary statements rather than a recurring feed-style source, more
analogous to the existing YouTube-interview verbatim-quote precedent than
a new scan target).

**Budget note:** ~14 web searches/fetches total this cycle (2 watching
rechecks, 3 discovery-sweep queries, 2 dedup-checks, ~7 verification
searches/fetches across the two published stories), well under the
15/story cap; 2 stories published against the 4/cycle, 12/night caps.

## Cycle 2 — 23:14-23:22 UTC

Very short window this cycle (deadline 23:44Z, cycle started 23:14Z after
ingest). Prioritized a fast, high-confidence in-place update over starting a
brand-new story that could not clear full verify+publish protocol in the
remaining time.

**Watching rechecks (step 1):** skipped both — neither `meta-ai-chip-production`
(Iris/MTIA) nor `openai-safety-heidecke-departure` (Heidecke) was mentioned in
this cycle's fresh ingest (`data/raw/2026-07-14/`, checked via grep for
"heidecke"/"iris"/"MTIA" — no hits), and both were already rechecked in
cycle 1 per the run order. Left alone as instructed.

**Tier-0 sweep (step 2):** skipped — no fresh Tier-0 html content flagged by
raw ingest beyond what cycle 1 already covered, and no candidate story this
cycle pointed to one. Flagging again for a later cycle if window allows.

**Discovery sweep (step 3, queries at index 3-5):**
- "AI model capabilities claim disputed" — miss (securities-litigation and
  AI-washing explainer content, nothing that's a discrete new AI-vertical
  news event).
- "machine learning paper significant result" — miss (evergreen
  "most-cited papers" listicles, no new candidate).
- "AI research replication failure" — miss (reproducibility-crisis
  explainer/survey content, no new candidate).
Same pattern as cycle 1's queries: this pool consistently surfaces
explainer/tracker content rather than news events. Recommend the owner
reconsider the query pool (flagged again, third night running).

**Triage / candidates considered:**
- OpenAI's first substantive public response to Apple's trade-secrets suit
  ("not aware of any evidence that this complaint has merit," 14 July,
  Bloomberg/Ed Ludlow exclusive quote, corroborated verbatim by TechCrunch,
  9to5Mac, MacRumors, Android Headlines) — dedup-check: moderate match to
  `apple-openai-trade-secrets-lawsuit` (published 10 July). This was
  precisely the first item on that story's `watch` list. **Outcome (a):
  in-place update** — published as an update to both EN/TR articles (see
  below).
- Illinois Gov. Pritzker signs the "Artificial Intelligence Safety Measures
  Act" (per gnews-ai-safety-and-evaluations, first US law mandating AI
  safety audits) — genuinely new, in-scope (AI regulation), looks
  well-documented (state government action) — not pursued this cycle for
  time; flagging as a strong candidate for the next cycle.
- "GPT-5.6 Sol" reports of unprompted/autonomous file deletion — potentially
  a real AI-safety-incident story, but surfaced only via low-tier aggregator
  sites (Whalesbook, The Tech Buzz) this cycle; needs a primary/Tier-2 source
  check before triage, not pursued for time — flagging for a later cycle.
- Axios exclusive: Grok also falls for the same jailbreak that tricked
  ChatGPT into generating sexual/graphic images — in-scope AI-safety story,
  single-sourced (Axios exclusive) this cycle — not pursued for time,
  flagging for a later cycle to check for a second source.
- PrismML Bonsai 27B model release (Qwen3.6-based, claims on-device iPhone
  fit) — model-release candidate, not pursued for time.

**Verification notes — published update:**
- `apple-openai-trade-secrets-lawsuit`: treated OpenAI's 14 July statement
  as `confirmed` despite tracing to one origin outlet (Bloomberg/Ed Ludlow
  exclusive) — this is an attributed, on-record company statement (not an
  anonymous-sourced claim), and TechCrunch's independent write-up was
  fetched directly and confirms the quote is relayed verbatim and correctly
  attributed to Bloomberg, not independently obtained. This is a company
  making a public statement about itself, which the wire-exclusive rule's
  underlying concern (single unverifiable insider account) doesn't apply to
  in the same way it does for e.g. the still-watching Meta chip-memo story.
  Added as a new claim, 2 new source entries, and an "Update, 14 July 2026"
  section (after "Why it matters," matching the `meta-muse-image-consent`
  precedent) to both EN and TR articles. Ledger `watch` item 1 kept open —
  this is public commentary, not OpenAI's formal court filing (answer/motion
  to dismiss), which is the item that watch entry is actually tracking.

**Style-gate notes:**
- style-gate: apple-openai-trade-secrets-lawsuit EN pass / TR pass — no
  changes to headline or TL;DR (still accurate and falsifiable for the
  underlying story), no padding introduced, TR update section mirrors the
  EN update section's structure and content natively.

**Discovery logging:** no new unregistered evidence domains this cycle
(Bloomberg and TechCrunch are both already registered sources).

**Budget note:** ~5 web searches/fetches this cycle (2 grep-based watching
checks needing no live search, 3 discovery-sweep queries, 1 web fetch + 1
web search + 1 dedup-check for the update) — well under budget; 0 new
stories published this cycle (1 in-place update to an already-published
story, which doesn't count against the 4/cycle new-story cap), 2 stories
published tonight overall against the 12/night cap.
