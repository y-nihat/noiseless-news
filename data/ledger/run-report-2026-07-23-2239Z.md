# Night run report — 2026-07-23

## Cycle 1 — 22:42-22:51 UTC

**Note:** this cycle's deadline was unusually tight (23:12 UTC, ~30 min from
start) — scope was cut accordingly: parallelized watching-story rechecks and
the Tier-0/discovery sweeps across sub-agents, then verified and published one
high-confidence, clean-dedup story rather than rushing multiple.

### Watching stories (re-checked, all still watching)

- `anthropic-meta-compute-deal` — re-checked (1 search). CNN's own source now
  explicitly calls the ~$10B figure "speculative" rather than confirming it —
  reinforces rather than closes the independence gap. Still watching.
- `meta-ai-chip-production` — re-checked (1 search). Resolved last cycle's open
  TechTimes lead ("Broadcom Now Designs Chips for Three Rivals"): confirmed via
  search summary to be Reuters-memo-derived, not independent. No change to
  gate. Still watching (14th consecutive night unchanged).
- `openai-safety-heidecke-departure` — re-checked (1 search). No new
  independent source; effective-departure date (2026-07-24) is now 1 day out —
  flagged as next cycle's natural check point for post-departure coverage.
  Still watching.

### Tier-0 sweep

Swept all active Tier-0 `html`-type sources in three parallel groups.
- **Published:** AMD/Anthropic partnership (see below).
- **Notable but stale/out-of-window or lower priority, not opened this
  cycle:** Meta's Genesis Mission / Segment Anything post at Lawrence Berkeley
  Nat'l Lab (Jul 21); Kimi K3 + PerceptionBench (Jul 16, already ledgered);
  ByteDance Seed Audio 1.0 (Jul 20); Z.AI/Zhipu Chinese-chip-only datacenter
  completion (Jul 20-21, via search fallback); EU Commission's AI Act
  transparency-obligations guidelines (Jul 20). Flagging all for next cycle's
  triage consideration given remaining time this cycle.
- **Known blockers handled per instructions:** xAI (site-scoped search used —
  also surfaced an unverified "SpaceXAI" branding signal in search snippets,
  needs direct confirmation before treating as news, we already have
  `xai-spacexai-rebrand` published from 07-18 so likely just stale snippets);
  Zhipu/Z.ai (site-scoped search used, JS-rendered page confirmed still
  empty on direct fetch).
- **Inaccessible this cycle:** DeepSeek News (fetch resolved to API-docs
  landing page, not the news index — retry with corrected URL next cycle);
  StepFun, Baidu Research ("under maintenance"), Tencent AI Lab (resolved to
  unrelated Yuanbao assistant page), MiniMax News (nav-only, no dated posts) —
  all returned no usable dated content.

### Discovery sweep (queries at index 0-2 of `recurring_queries`)

- "AI model release announcement" — **noise**: generic aggregator/roundup
  sites recapping already-ledgered launches.
- "open source LLM release weights" — **noise**: stale listicle sites;
  confirmed Kimi K3 weights still expected ~Jul 27, already tracked.
- "LLM benchmark results independent evaluation" — **noise**: generic
  benchmark-methodology commentary, no concrete new evaluation event.
- No standalone candidate came from the discovery sweep this cycle; the raw
  ingest scan (below) was far more productive. Tuning note: these three
  queries have returned mostly noise across recent cycles per the recurring
  pattern — worth flagging to the owner as candidates for rotation/refinement
  in `policy/discovery.yaml` if this persists.

### Raw ingest triage — candidates considered

1. **AMD/Anthropic compute deal** — dedup-check clean → **published** (below).
2. **White House/OSTP accuses Moonshot AI of distilling Anthropic's model to
   build Kimi K3, alleges illegal Nvidia GB300 access via Thailand; Treasury
   sanctions threat** — dedup-check: moderate match (0.455) with
   `kimi-k3-release` → correctly flagged as a same-saga follow-up
   (`follows: kimi-k3-release`) rather than standalone or skip. **Skipped this
   cycle for time** — legal/allegation claims need the extra care of
   verification.md §3's litigation rule (attribute strictly to the accusing
   party); left for a future cycle with fuller budget rather than rushed.
3. **Samsung in talks for ~€1B stake in Mistral AI** (~€20B valuation) —
   dedup-check clean, two independent sources (FT via Techmeme + the-decoder's
   own writeup) per discovery agent. **Skipped this cycle for time** — good
   candidate for next cycle, not yet verified.
4. **Poolside Laguna S 2.1 open-weight coding model** — dedup-check: moderate
   match (0.375) with `meta-muse-spark-1-1`, but different company/product —
   coincidental phrase overlap ("agentic coding model"), would justify
   standalone if pursued. **Skipped this cycle for time.**
5. **Reddit considering cutting Google's AI-training data access** —
   dedup-check: moderate match (0.375) with
   `eu-google-ai-android-interoperability`, unrelated topic, would justify
   standalone if pursued. **Skipped this cycle for time.**
6. **Publishers/author copyright lawsuit vs Google over Gemini training
   data** — only one source found (UA.NEWS via GNews) — **not opened**, needs
   a second independent outlet before it's even a firm candidate.

### Published this cycle

**`amd-anthropic-compute-deal`** — AMD commits up to $5 billion to Anthropic in
an AI chip partnership; up to 2GW of AMD Instinct MI450-series GPUs planned for
Claude, first gigawatt in H1 2027. Dedup-check clean (distinct from the
still-watching `anthropic-meta-compute-deal`, a separate Meta-related story,
and from `meta-ai-chip-production`, an unrelated Meta chip story). Origin is a
Tier-0 joint AMD/Anthropic press release (ir.amd.com) — stronger than the
minimum bar since every load-bearing figure is confirmed directly against the
primary source rather than only corroborated secondary reporting. Fresh
verifier + fresh adversarial falsifier both ran independently; falsifier
re-fetched the primary release directly, found no contradictions, and flagged
two caveats reflected in the ledger: no Anthropic-side mirror of the
announcement (acceptable per policy — one Tier-0 primary suffices), and an
unverified secondary claim about a separate large hardware-purchase commitment
(deliberately excluded from the published claim set for lack of a citable
source).
style-gate: amd-anthropic-compute-deal EN pass / TR pass — headline is
falsifiable and claim-supported, TL;DR stands alone, no padding, Turkish
mirrors structure with natural phrasing and no calques.

### Budget note

~10 web searches/fetches total across watching-story rechecks, Tier-0 sweep,
discovery sweep, and the AMD/Anthropic verify+falsify passes; 1 of 4 stories
this cycle, 1 of 12 for the night. Well under the 15-searches/story cap. Time
was the binding constraint this cycle (30-minute window), not search budget —
four other clean or promising candidates (Samsung/Mistral, Moonshot/Kimi K3
follow-up, Poolside, Reddit/Google) are carried forward, not dropped, for the
next cycle to pick up.
