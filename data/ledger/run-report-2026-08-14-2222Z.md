# Night report — 2026-08-14 (2222Z start)

## Cycle 1 — 22:24-22:33 UTC

Opened with an unusually short window (deadline 22:57 UTC, ~33 minutes total).
Given the time budget, this cycle prioritized re-checking every `watching`
ledger entry (work order step 1) over the Tier-0 sweep, discovery sweep, and
new-story triage/verify/publish (steps 2-6), which were skipped entirely this
cycle. No published entries had `open_obligation: true` with a `revisit_after`
date at or before today (checked all 16; earliest is 2026-08-17), so no
obligation re-checks were due.

### Watching re-checks (9 entries, 1 web search each)

- **alibaba-qwen-revenue-share-plan**: watching, unchanged. Revenue-share rate
  still under negotiation, no Alibaba on-record statement. revisit_after
  2026-08-15 unchanged.
- **anthropic-meta-compute-deal**: watching, unchanged. $10B deal-size figure
  still NYT-only; CNN still frames it as speculative. No Meta/Anthropic
  statement after ~4 weeks.
- **deepseek-funding-round-pause**: watching, unchanged. Round resumed
  (~$8B/~$74B pre-money per Bloomberg), signing still "expected late August,"
  not done. Original pause claim remains Bloomberg-"people familiar" only.
- **deepseek-v4-pro-0813-quiet-update**: watching, substance grew but not
  enough to clear the gate. Vendor-reported benchmark deltas now public
  (DeepSWE 12.8→62.7, CyberGym 52.7→83.3, Terminal Bench 2.1 72.1→87.9) but
  TechTimes' own framing is "await independent proof" — no independent
  benchmark run, no changelog/blog entry. Flagging as the closest-to-ready
  watching entry for a cycle with time for a full verify pass (would publish
  as a labeled vendor-claim announcement, not confirmed capability).
- **meta-ai-chip-production**: watching, unchanged. Single-source Reuters
  memo, September start now ~2.5 weeks out.
- **nvidia-openai-250b-guarantee**: watching, unchanged. Attempted a direct
  WebFetch of CNBC's own article (the one lead flagged repeatedly since
  2026-08-11 as potentially resolving the independence question) — blocked
  HTTP 403, same as every prior attempt. Search-synthesis again claimed
  independent CNBC confirmation; not accepted without fetched text, per
  standing policy on this entry.
- **openai-additional-agent-containment-escapes**: watching, unchanged.
  Single Reuters exclusive, no second outlet, no openai.com post.
- **openai-astra-ten-math-proofs**: watching, unchanged. Thomas Bloom remains
  the only named independent mathematician; still an interest reaction, not a
  soundness assessment. No formal peer review yet.
- **openai-safety-heidecke-departure**: watching, unchanged. Over a month with
  zero independent corroboration. The 2026-08-11 drop-consideration flag
  stands (revisit if another 1-2 weeks pass unchanged); ~3 days into that
  window, not yet due.

### Skipped this cycle (time budget)

Tier-0 sweep, discovery sweep, and new-story triage/verify/publish were not
attempted — the ~33-minute window went entirely to the watching re-check
round above plus one direct-fetch attempt. No new candidates were opened, no
articles were published or updated this cycle. Zero discovery-sweep queries
run; nothing to log there.

### Budget note

9 WebSearch calls + 1 WebFetch (blocked) this cycle. Well under all budget
caps; the binding constraint was cycle wall-clock, not spend. 0/4
stories/cycle used, 0/12 stories/night used.

## Cycle 2 — 23:03-23:06 UTC

Another short window (deadline 23:32 UTC, ~28 minutes total after fresh
ingest landed at 23:03). Per work order step 1, did not blanket re-check all
watching entries (that was done exhaustively in cycle 1) — only re-checked
entries this cycle's fresh ingest actually touched.

### Watching re-checks triggered by fresh ingest (2 entries)

- **deepseek-v4-pro-0813-quiet-update**: still watching, but the story has
  changed shape materially. Reuters and Nikkei Asia both report DeepSeek's
  own on-the-record announcement that V4-Pro-0813 formally exited preview
  today, with a stated price change (peak output tokens to $3.96/M from
  $0.87/M) and an agent-capability focus. This is now a company-confirmed
  launch covered independently by two tier-0/1 outlets — the launch+pricing
  facts look verify-ready. The earlier vendor benchmark deltas (DeepSWE/
  CyberGym/Terminal Bench) remain unconfirmed by any independent evaluator.
  No time this cycle to run the full verify/falsify/bilingual-write/editor-gate
  protocol safely inside the remaining window, so left as watching with a
  detailed handoff note flagging it as top priority for the next cycle.
- **alibaba-qwen-revenue-share-plan**: still watching. SCMP (direct source,
  breaking from the Reuters-relay chain this entry has tracked for a week)
  reports Alibaba's actual published Qwen3.8-Max licence terms — and they
  are a revenue-threshold commercial-licence gate ($50M/12mo trailing
  revenue trigger), not the percentage-of-revenue-share structure the
  original Reuters exclusive described. This resolves the open question but
  not as originally framed, so it needs a fresh verify pass against
  Alibaba's own licence text next cycle rather than a straight "confirmed"
  flip. `revisit_after` unchanged at 2026-08-15.
- Remaining 7 watching entries: not touched — fresh ingest did not mention
  them, so per step 1 they were left alone for the scheduled final-cycle
  recheck.

### Tier-0 sweep, triage, verify/publish

Skipped this cycle — the window did not leave enough time to safely run the
full multi-agent verify+falsify+editor-gate+bilingual-write protocol for even
one new story after the mandatory discovery sweep and the two triggered
watching rechecks above. No new candidates were opened; 0 stories
published/updated this cycle.

### Discovery sweep (queries 4-6 of policy/discovery.yaml, index 3)

- "AI model capabilities claim disputed" — miss (generic regulatory/AI-washing
  background pieces, nothing timely).
- "machine learning paper significant result" — miss (evergreen
  historical/landmark-papers content, nothing dated within 72h).
- "AI research replication failure" — miss (evergreen replication-crisis
  background, nothing dated within 72h). All three queries returned
  background/reference content rather than fresh news this cycle; no
  candidates opened from discovery.

### Budget note

2 WebSearch calls (watching rechecks) + 3 WebSearch calls (discovery sweep) =
5 calls this cycle. Well under caps; wall-clock was again the binding
constraint, not spend. 0/4 stories/cycle used, 0/12 stories/night used
cumulative (still 0 after cycle 1 and cycle 2).

## Cycle 3 — 23:37-00:00 UTC

Another short window (deadline 00:07 UTC, ~30 minutes total). Per step 1, did
not blanket re-check watching entries — only touched the two this cycle's
fresh ingest actually mentioned, one of which turned out to be a false
positive on inspection.

### Watching re-checks

- **deepseek-v4-pro-0813-quiet-update** (flagged by cycle 2 as top priority —
  launch+pricing looked publication-ready): spent the bulk of this cycle's
  budget here. DeepSeek's own changelog
  (https://api-docs.deepseek.com/updates) now carries a dated 2026-08-13 entry
  for the GA rollout, with agent-capability framing, vendor benchmark figures,
  and the stated peak/off-peak pricing change — this clears the Tier-0
  primary-source gap that had been the last missing piece since 2026-08-10/11.
  But a falsification search surfaced a real complication rather than a clean
  green light: South China Morning Post (Tier-2, already an
  unregistered-source candidate from 2026-08-07) published an independent
  piece titled "DeepSeek's updated V4 Pro AI model struggles on benchmarks,
  shines in cybersecurity" — a direct dispute of the vendor's broad-gains
  framing, not just an "unverified" caveat. Also found several low-quality
  SEO/content-farm domains (novcog.us.com, lovableapp.org, kingy.ai,
  thecherrycreeknews.com) templated around an unrelated "Fable 5" product —
  not registered, not proposed, not used as evidence, but they independently
  described DeepSeek's original in-app release note as posted then quietly
  removed the next day, and referenced a separately-reported *future* price
  increase with no figure/timeline that sits oddly next to the changelog's
  own specific $3.96/M peak figure. Given the newly surfaced SCMP dispute and
  pricing ambiguity, did not attempt the full verify/adversarial-falsify/
  bilingual-write/editor-gate pass this cycle — publishing the benchmark
  claims as vendor-claim-labeled would not resolve SCMP's direct "struggles"
  finding, which needs to be read in full first. Detailed handoff written into
  the ledger entry's notes for the next cycle: read SCMP in full, re-check
  DeepSeek's changelog for pricing consistency, then very likely
  publication-ready as a launch+pricing article (confirmed) with a labeled
  vendor-claim/disputed capability section. Still watching.
- **alibaba-qwen-revenue-share-plan**: grep hits in this cycle's fresh
  ingest (arxiv baseline-model mentions, gnews "Qwen" hits) were checked and
  are false positives — no substantive new coverage since cycle 2's SCMP
  licence-terms finding. Left untouched per step 1; revisit_after 2026-08-15
  unchanged.
- Two other grep hits worth recording as false positives so the pattern is
  visible for tuning: "Anthropic...Meta" matched a Bernie Sanders open letter
  (unrelated to the anthropic-meta-compute-deal watching entry) and
  "containment/escape" matched a JD Supra piece on general AI evaluation
  incidents (unrelated to openai-additional-agent-containment-escapes). Both
  entries left untouched.

### Tier-0 sweep

Folded into the deepseek-v4-pro-0813-quiet-update recheck above (its two
remaining Tier-0 pages, /news and /updates, were the highest-value fetch
available this cycle). No other Tier-0 pages fetched — no other candidate
pointed to one, and time did not allow a broader sweep.

### Discovery sweep (queries 7-9 of policy/discovery.yaml, index 6)

Corrected an index-calculation slip mid-cycle: the work order's index formula
for cycle 3 is `((3-1)*3) mod 12 = 6`, i.e. queries 7-9 ("AI company funding
round announcement", "AI acquisition deal confirmed", "AI datacenter compute
investment"), not queries 10-12. Two queries were run against the wrong
index first; both sets are logged here for the record, but only the correct
one counts against the query-pool tuning note.

- "AI company funding round announcement" — miss (roundup/aggregator content;
  OpenAI's $122B round and Prometheus's $12B round are both already old news
  by the time this ran, not fresh 72h candidates).
- "AI acquisition deal confirmed" — miss for AI-vertical purposes; surfaced
  one dated item (OpenAI acquired presentation startup NextSlide, reported
  2026-08-10) that is real but four days old, minor, and not chased further
  given the time budget — noting it here rather than silently dropping it in
  case a future cycle's ingest surfaces it with more substance.
- "AI datacenter compute investment" — miss (evergreen hyperscaler-capex
  analysis, nothing dated within 72h tied to a single verifiable event).
- (Off-index, run before the mistake was caught) "AI regulation law passed" —
  miss (background/evergreen). "AI safety evaluation report published" — miss
  (report is from February 2026, not fresh). "AI incident harm report" —
  surfaced the UK AISI incident report (INC-2026-07-28-01, published
  2026-08-04) documenting unsanctioned agent actions during a cyber
  evaluation; more than 72h old and already the kind of story this ledger
  would likely have opened on publication, not chased further this cycle but
  worth a fast dedup-check next time it resurfaces in a feed.

### Triage, verify/publish

Skipped this cycle. With a ~30-minute window, budget went to resolving the
deepseek-v4-pro Tier-0 gap and running the discovery sweep (after correcting
the index mistake); no time remained to safely run dedup-check plus the full
multi-agent verify+falsify+bilingual-write+editor-gate protocol on a new
candidate. 0 stories published/updated this cycle.

### Budget note

7 WebSearch calls (1 falsification search + 3 off-index discovery + 3
on-index discovery) + 2 WebFetch calls (DeepSeek /news, /updates) this cycle.
Well under all caps; wall-clock remained the binding constraint, not spend.
0/4 stories/cycle used, 0/12 stories/night used cumulative (still 0 after
cycles 1-3).

## Cycle 4 — 00:12-00:23 UTC

Fresh ingest at 00:12Z was light (only new items since the last check: 5
files — three GNews queries, r/LocalLLaMA, Techmeme). Per step 1, did not
blanket re-check watching entries; checked the two entries fresh ingest
plausibly touched.

### Watching re-checks triggered by fresh ingest

- **deepseek-v4-pro-0813-quiet-update**: this cycle's main focus (see below)
  — the fresh ingest didn't newly mention it, but it was carried forward as
  the explicit top-priority handoff from cycles 1-3 ("very likely
  publication-ready pending SCMP reconciliation"). Resolved and published
  this cycle.
- **openai-safety-heidecke-departure**: fresh ingest's "OpenAI is losing its
  second executive this week" (The Verge) turned out to be about a
  *different* pair of executives (COO Brad Lightcap, CRO Denise Dresser —
  business side) than this entry's safety-org reshuffle (Mia Glaese/Saachi
  Jain). Confirmed unrelated; left this entry untouched, still watching,
  still wire-exclusive-blocked on the Wired/Mark Chen account.
- **openai-additional-agent-containment-escapes**: "OpenAI's Rogue Agent Hack
  Sparks Internal Safety Crisis" (via_outlet "The Tech Buzz" = techbuzz.ai,
  a `blocked_evidence_domain`) appeared in the safety-and-evaluations GNews
  feed. Not traced or used per the blocklist — noted here only as a
  discovery-domain hit, not as evidence. Entry left untouched.

### Published this cycle: deepseek-v4-pro-0813-quiet-update

Full multi-agent protocol run: dedup-check (clean — only this story's own
watching entry matched, as expected), fresh verifier and fresh adversarial
falsifier sub-agents run in parallel, both live-fetching primaries
themselves (DeepSeek's GA announcement page and updates log, DeepSeek's
pricing page, Nikkei Asia, Artificial Analysis, and the SCMP article in
full) rather than trusting the ledger's accumulated secondhand notes.

- **Launch + pricing (confirmed)**: V4-Pro exited API preview to GA across
  app/web/API on 13 August 2026; API output-token pricing rises from a flat
  $0.87/M to a peak/off-peak split ($3.96/M peak, $1.98/M off-peak),
  effective 16:00 UTC 16 August. Tier-0 primary (DeepSeek's own dated
  changelog entry) plus independent Reuters (via TradingView) and Nikkei
  Asia coverage.
- **Vendor benchmark deltas (vendor-claim)**: Terminal-Bench 2.1 87.9,
  DeepSWE 62.7, CyberGym 83.3, NL2Repo 61.5, HLE 42.7/60.0 — DeepSeek's own
  figures, live-fetched, not independently reproduced.
- **SCMP dispute — resolved as non-contradictory**: the verifier and
  falsifier independently read the SCMP article in full and found its
  "struggles on benchmarks" framing rests on *different* benchmarks
  (Artificial Analysis Intelligence Index — score 53, live-fetched by us and
  independently matching SCMP's cited figure; Vals AI Index rank) than
  DeepSeek's own reported deltas, so it's not a direct rebuttal — published
  as attributed single-source color, not as `disputed`. The specific Vals AI
  numeric rank was not independently live-fetched by us, so it's presented
  as SCMP's reporting rather than our own confirmed figure (live-fetch rule,
  verification.md §5).
- **Rejected claims**: a rival "12 August quiet release" date and a
  retracted-release-note theory, both sourced only to unregistered
  content-farm domains, had no Tier 0-2 corroboration and were dropped
  entirely. A NIST/CAISI cybersecurity figure was excluded because it tested
  the April preview build, not this GA release — would have misattributed
  stale data.
- style-gate: deepseek-v4-pro-0813-quiet-update EN pass / TR pass (headline
  quantified at 4.5x and falsifiable; TL;DR standalone; no padding found in
  either language; TR reads as a natural mirror, not a calque).

### New candidate opened (not verified/published this cycle)

- **openai-lightcap-dresser-departures**: OpenAI COO Brad Lightcap
  (2026-08-11) and CRO Denise Dresser (2026-08-13) both departed within
  days — surfaced via The Verge in this cycle's GNews regulation/policy
  feed. dedup-check clean (zero matches, confirmed distinct from the
  still-watching Heidecke safety-org entry). CNBC and Fortune both cover it
  with apparently independent bylines. Opened as `candidate` with a detailed
  handoff for a fast verify pass next cycle rather than rushed this cycle —
  no time remaining after the deepseek publish to run the full protocol
  safely before the deadline.

### Discovery sweep (queries 10-12 of policy/discovery.yaml, index 9)

- "AI regulation law passed" — hit for background/tuning purposes but not
  chased as a story: EU AI Act high-risk/transparency rules took effect
  2 August (already past 72h), California's Frontier AI Safety Act passed
  its final assembly vote 10 August (awaiting governor's signature — an
  `open_obligation`-flavored story worth a future cycle once signed).
- "AI safety evaluation report published" — hit: Future of Life Institute's
  "AI Safety Index — Summer 2026" (ranks nine labs; Anthropic leads,
  xAI/DeepSeek/Mistral get failing grades). Not chased this cycle — no
  specific dated news hook within 72h beyond the index's general release;
  worth a look next cycle if it hasn't already been covered.
- "AI incident harm report" — hit, but same UK AISI incident report
  (INC-2026-07-28-01) already logged as a miss in cycle 3's off-index sweep;
  no new information. Confirmed still not chased.

### Tier-0 sweep

Folded into the deepseek verification above (DeepSeek's own changelog,
GA announcement page, and pricing page were the highest-value Tier-0 fetches
available this cycle); no separate broader sweep attempted given the time
spent on the priority publish.

### Budget note

2 parallel sub-agent research passes (verifier + falsifier, ~9 and ~8 tool
calls respectively) + 1 WebSearch + 1 WebFetch directly by the main cycle +
4 WebSearch calls for the OpenAI-executive check and discovery sweep this
cycle. Well under the 15-searches/story and 120-turns/cycle caps. 1/4
stories published this cycle, 1/12 stories/night used cumulative (first
publish of the night, after three consecutive cycles that could not fit a
full verify+publish pass in their time windows).
