# Night run report — 2026-07-19 (started 2026-07-18 22:29Z)

## Cycle 1 — 22:32-22:41 UTC

Note: this cycle's window was unusually short (~30 min from ingest to deadline).
Spent it on the mandatory watching-rechecks, the full Tier-0 sweep, and the
discovery sweep; deliberately did not start a full verify+publish pass — with
the remaining budget, a fresh-verifier + adversarial-falsifier + bilingual
editor-gate pass could not be safely finished before the 23:02Z deadline for
any of the candidates found. Two strong candidates are queued for cycle 2.

### Watching stories re-checked (2/2)

- `openai-safety-heidecke-departure`: re-checked, 1 web search. No change —
  same set of outlets (PYMNTS, TheNextWeb, TechTimes, Storyboard18, Gizmodo,
  Windows Forum, inshorts) all still trace to the single Wired-sourced Mark
  Chen memo/quote. No independent OpenAI/Heidecke contact, no OpenAI.com or
  LinkedIn title confirmation for Glaese/Jain, no Heidecke statement. Gate
  still fails on wire-exclusivity (verification.md §2). Still watching;
  reported effective-departure date 2026-07-24 is 5 days out.
- `meta-ai-chip-production`: re-checked, 1 web search (site-scoped to exclude
  reuters.com). No change — same set of outlets (precedenceresearch.com,
  Threads, Lumien, Yahoo Finance, qz.com, FullStackEvolved, letsdatascience,
  DCD, MLQ, CNBC-citing-Reuters) still repeat the same 9 July Reuters/
  internal-memo details verbatim. No Meta statement, filing, or independent
  second source. Gate still fails on independence. Tenth consecutive night
  unchanged; TrendForce-corroborated narrower sub-story (chip generation/
  node/half-year timing only) remains available to split out as a smaller
  publishable unit if the owner wants it — flagged again, still unactioned.

### Tier-0 sweep (17/17 active html sources)

Ran as three parallel fetch passes covering last ~3 days (since 2026-07-15).

- **Anthropic News, Meta AI Blog, Mistral AI News, Allen Institute AI Blog**:
  nothing newer than 2026-07-14 (already covered by existing ledger/articles).
- **Cohere Blog**: two in-window posts, both already published
  (`cohere-university-of-toronto-partnership`, 2026-07-16) or thin ("total
  cost of AI ownership" — not news, cost-explainer content, skipped).
- **xAI News**: blocked direct fetch (403, known blocker) — fallback
  site-scoped search found "Grok Build is Now Open Source" (~2026-07-15) —
  **new candidate, not yet in ledger** (dedup-check: no match). Also
  resurfaced Grok 4.5 (already published).
- **Zhipu AI (Z.ai)**: JS-rendered, fallback search — only financial/
  corporate coverage (Bloomberg: on track for $1B annual China sales,
  2026-07-17) and a ZCode-harness mention outside the window; no new
  product announcement in-window.
- **NIST AI**: nothing in window.
- **UK AI Security Institute**: new post, "How Far Behind the Frontier are
  Leading Open Weight Models on Cyber?" (2026-07-17) — Tier-0/AISI research
  finding on GLM-5.2 and DeepSeek V4-Pro cyber capability vs. closed models.
  **New candidate, not yet in ledger** (dedup-check: no match).
- **OECD.AI**: homepage only points at a blog index; no dated items
  surfaced this pass — worth a direct /en/wonk fetch next time.
- **DeepSeek News**: no in-window news items; only an undated
  deepseek-chat/deepseek-reasoner model-name deprecation notice
  (effective 2026-07-24) — not news-worthy on its own, noted for awareness.
- **Moonshot AI (Kimi)**: "PerceptionBench" (2026-07-16, new benchmark) —
  thin, likely fails residual-substance test on its own; and confirmation
  that Kimi K3's weights are slated for publication 2026-07-27 — relevant as
  a future update trigger for the already-published `kimi-k3-release` entry,
  not a new story.
- **MiniMax, StepFun, Tencent AI Lab**: fetches returned JS-shell/nav-only
  content or resolved to the wrong page (Tencent → Yuanbao product page) —
  no usable content. These three need a non-static-fetch strategy; flagging
  for the source-lifecycle review, not actioned this cycle.
- **ByteDance Seed**: two posts, both predate the window (2026-07-07/08),
  already covered by existing `bytedance-edgebench-scaling-law` ledger entry.
- **Baidu Research**: page down for maintenance, no content.
- **European Commission Digital Strategy News**: one in-window item
  (Google/Android AI interoperability guidance, 2026-07-16) — dedup-check
  confirms moderate match to already-published `eu-google-ai-android-
  interoperability`; same event, no new load-bearing detail found in this
  pass, no update made.

### Discovery sweep (queries 1-3 of 12, index 0)

1. "AI model release announcement" — **hit**: Moonshot Kimi K3 (Bloomberg,
   CNBC, TechCrunch, 2026-07-16/17) — already published
   (`kimi-k3-release`).
2. "open source LLM release weights" — **hit** (same underlying story):
   Kimi K3 weights slated for 2026-07-27 (Simon Willison) — noted above as
   an update trigger, not new.
3. "LLM benchmark results independent evaluation" — **miss**: only evergreen
   leaderboard/explainer content (Vellum, Evidently AI, LangChain), nothing
   dated in-window.

### Candidates queued for cycle 2 (not yet triaged/verified)

- **xAI open-sources Grok Build** (coding agent/TUI, Apache 2.0, GitHub
  `xai-org/grok-build`) — includes a self-disclosed remediation of an
  earlier privacy issue (uploaded-repo data to Google Cloud without clear
  consent, since deleted per xAI). Announcement claim is Tier-0-confirmable
  via x.ai's own post; the privacy-remediation claim needs origin tracing
  (who first reported the syncing issue, when) before it can be more than
  an attributed xAI self-statement. Dedup-check: no match, clear to open.
- **UK AISI: leading open-weight models (GLM-5.2, DeepSeek V4-Pro) closing
  the cyber-capability gap with closed models** — Tier-0 primary research
  finding, in scope (§0 AI safety/eval). Dedup-check: no match, clear to
  open. Needs claim decomposition (which specific benchmark deltas are
  load-bearing) before verification.

### Discovery logging

No unregistered evidence domains were used as evidence this cycle (nothing
published/updated). Query-pool note: queries 1 and 2 both surfaced the same
Kimi K3 story — some redundancy between the "release announcement" and
"open weight release" query pair worth watching over more cycles before
deciding whether to diversify one of them.

### Budget note

0 stories verified/published this cycle (0/4 cycle cap, 0/12 night cap
used). ~8 web searches + 3 tier-0-sweep sub-agent fetch passes + 2 dedup-
checks spent. No usage-limit errors. Two candidates queued with dedup
already cleared, ready for a fresh verifier in cycle 2.

## Cycle 2 — 23:07-23:16 UTC

Note: this cycle's window was also short (~30 min from ingest to deadline).
With two candidates already dedup-cleared from cycle 1, spent the cycle on
one full verify+publish pass rather than spreading thin across sweep +
discovery + both candidates — deliberately skipped the Tier-0 sweep,
discovery sweep, and fresh triage this cycle to protect enough time for a
safe, complete multi-agent verification + bilingual editor-gate pass on one
story. The second queued candidate (xAI Grok Build) is requeued for cycle 3,
unchanged and still dedup-cleared.

### Watching stories

Not re-checked. Fresh ingest mentioned Meta Iris/MTIA (Motley Fool, Yahoo
Finance, Longbridge, Globe and Mail, Pluang — all 2026-07-18, "production in
September... 14 gigawatts") but these are the same outlets/framing already
logged repeatedly in `meta-ai-chip-production`'s history as downstream of
the single 9 July Reuters/internal-memo exclusive (Motley Fool specifically
already noted as a repeat outlet since the 2026-07-13 night cycles) — no
live search spent confirming this, treated as a known pattern rather than a
genuine new-information trigger. `openai-safety-heidecke-departure`: no
mention in this cycle's fresh ingest, left untouched per instructions.

### Tier-0 / discovery sweep

Skipped this cycle (time budget, see note above). Cycle 1 already ran the
full 17-source Tier-0 sweep and discovery queries 1-3; cycle 3 or a later
cycle should pick up discovery queries at index 6 (((3-1)*3) mod 12) rather
than repeating 3-5.

### Candidates considered

- **UK AISI: open-weight models (GLM-5.2, DeepSeek V4-Pro) close cyber
  capability gap to 4-7 months** — dedup-check re-run this cycle: no match
  (also cleared in cycle 1). **Published.** See verification notes below.
- **xAI Grok Build** (coding agent/TUI, Apache 2.0, self-disclosed privacy
  remediation) — dedup-check re-run this cycle: no match, still clear to
  open. **Skipped this cycle for time** (would need to trace the privacy-
  issue's origin report, more research-intensive than the AISI story) —
  requeued for cycle 3.

### Verification notes — `aisi-open-weight-cyber-gap` (published)

Fresh verifier fetched AISI's primary blog post
(aisi.gov.uk/blog/how-far-behind-the-frontier-are-leading-open-weight-
models-on-cyber, published 2026-07-17) directly and cross-checked figures
against The Decoder's independent write-up (2026-07-18, matching numbers).
Fresh adversarial falsifier independently re-fetched the AISI primary text
twice and: (1) upgraded the DeepSeek-V4-Pro-vs-Sonnet-4.5 cyber-range detail
from "sourced only from the secondary outlet" to "confirmed verbatim in
AISI's own primary text"; (2) corrected a cost-attribution error (the $85
figure covers both Opus 4.5 and 4.6, not one model); (3) surfaced a new
caveat not in the verifier's report — AISI's comparison set excludes
Anthropic's more recent Opus 4.7/4.8 (both released before GLM-5.2 shipped),
so the measured gap is against an earlier Opus generation, not Anthropic's
frontier at test time; this is now stated as a caveat in the article body.
No contradicting reports, no stale/mismatched model versions, no spin beyond
the source found. All four load-bearing claims are `research` type,
`confirmed` against AISI's own primary evaluation (labeled in the article as
a non-peer-reviewed blog post, per verification.md §3). AISI (Tier-0) and
The Decoder (Tier-2) are both registered sources.

style-gate: aisi-open-weight-cyber-gap EN pass / TR pass (headline states a
falsifiable fact backed only by confirmed claims; TL;DR stands alone; no
padding/throat-clearing/empty closers; Turkish version mirrors structure
with natural phrasing, not calque).

### Discovery logging

No unregistered evidence domains used (AISI and The Decoder are both
already registered). No discovery-sweep queries run this cycle (see note
above) — nothing to log.

### Budget note

1 story verified/published this cycle (1/4 cycle cap, 1/12 night cap used).
~6 web searches/fetches across verifier + falsifier sub-agents, 2 dedup-
checks. No usage-limit errors. One candidate (xAI Grok Build) requeued for
cycle 3, dedup already cleared.

## Cycle 3 — 23:42-23:58 UTC

Note: with two candidates already queued (one dedup-cleared, one requeued from
cycle 2 for time), spent the full cycle on one verify+publish pass rather than
spreading across sweep + discovery + triage — same tradeoff as cycles 1-2 this
window has consistently been ~30 min end to end. Tier-0 sweep and discovery
sweep both skipped again this cycle for time; discovery queries still owed
starting at index 6 (queries 7-9: "AI company funding round announcement",
"AI acquisition deal confirmed", "AI datacenter compute investment").

### Watching stories

Not re-checked with a live search. Checked this cycle's fresh ingest
(gnews-ai-chips-and-compute.json) directly: `meta-ai-chip-production` — same
outlets already logged in cycle 2 (Motley Fool, Yahoo Finance, Longbridge,
Globe and Mail, Pluang, all 2026-07-18) plus one new item, "Anthropic and
Meta in Early $10 Billion Computing Deal Talks" (Briefs Finance,
2026-07-17T22:14Z) — a different claim (compute-deal talks, not the chip
production timeline) from a single low-tier outlet; not a trigger for this
entry and not independently pursued as its own candidate this cycle for time
— flagging for a later cycle's triage pass. `openai-safety-heidecke-
departure`: no mention in fresh ingest, left untouched per instructions.

### Candidates considered

- **xAI (SpaceXAI) open-sources Grok Build coding agent** — requeued from
  cycle 2. Dedup-check re-run this cycle with a refined title (reflecting
  the real story, see below): no match. **Published** as
  `xai-grok-build-privacy-incident`. See verification notes below.

### Verification notes — `xai-grok-build-privacy-incident` (published)

Cycle 1's original framing of this candidate ("self-disclosed remediation of
an earlier privacy issue") turned out to be wrong once a fresh verifier
actually traced the origin: the privacy incident was independently
discovered and published by security researcher **cereblab**
(wire-level/mitmproxy analysis with a reproducible canary-file proof,
published 12 July 2026, three days *before* SpaceXAI's 15 July open-source
release), not a self-disclosure. This materially changes the story from "we
found a small caveat in an open-source release" to "an independent
researcher caught a coding agent uploading full private repos, including
secrets, to the vendor's cloud regardless of a privacy toggle — the open-
source release came after."

Fresh verifier fetched the GitHub repo directly (Tier-0 primary — LICENSE,
README) and traced the incident's origin. Fresh adversarial falsifier
independently re-fetched both the GitHub repo and cereblab's gist directly
(not from search snippets), corroborated The Register's reporting, confirmed
the SpaceXAI rebrand (6 July 2026) is real and unrelated-but-relevant
context, and confirmed the timeline is internally consistent (cereblab
12 July → OECD.AI incident log 13 July → server-side fix same day → Musk's
deletion pledge 14 July → open-source release 15 July). The falsifier's one
substantive addition — that press reports (TechTimes, byteiota, Winbuzzer)
say the upload code path itself remains in the newly published source,
merely gated by a server-side flag rather than removed — was **deliberately
not included as a claim**: those three outlets were only found via search
this cycle, not independently re-fetched, and are unregistered, so under
this cycle's time budget they didn't clear the evidence bar. Logged as a
ledger watch item instead of forcing a citation, and logged in
`source_candidates.json` (techtimes.com now recurring a third time — case for
Tier-2 registration is building).

Four claims published: (1) the open-source announcement itself —
`confirmed`, GitHub primary + The Decoder; (2) the underlying upload
incident — `confirmed`, cereblab's primary technical writeup + The Register;
(3) xAI's deletion/remediation claim — `single-source`, attributed to a
Musk X post via The Register's reporting, explicitly not stated as our own
confirmed fact since neither we nor, per cereblab, anyone independent has
verified the underlying deletion; (4) cereblab's dispute of xAI's fix
framing — `disputed`, presented per verification.md §8 as a documented
disagreement rather than resolved either way. Benchmark comparison numbers
(Grok Build vs. Claude Code/Codex/Cursor) found only on low-authority
SEO/content-mill sites were excluded entirely — didn't clear even a labeled
vendor-claim bar.

cereblab is an unregistered individual researcher; used as evidence per
verification.md §1 (qualifies as Tier 1-2 technical primary given the
reproducible proof, independently re-fetched twice) with a note, logged in
`source_candidates.json` for a source-lifecycle review rather than a
straight registration proposal, since "individual security researcher gist"
doesn't fit the existing registry categories cleanly.

style-gate: xai-grok-build-privacy-incident EN pass / TR pass (headline
states only the two `confirmed` facts — open-source release + the
independently-discovered incident — not the disputed remediation claim,
per Gate 1's confirmed-claims-only rule; TL;DR stands alone; no
throat-clearing/padding/empty closers; Turkish mirrors structure with
natural phrasing, not calque, including the disputed-claim framing).

### Discovery logging

One unregistered evidence domain used this cycle: `gist.github.com`
(cereblab), logged above. techtimes.com logged again as a non-evidence
recurrence (dropped claim, not cited). No discovery-sweep queries run this
cycle (skipped for time, see note above).

### Budget note

1 story verified/published this cycle (1/4 cycle cap, 2/12 night cap used
across cycles 1-3). ~6 web searches/fetches across verifier + falsifier
sub-agents plus 2 targeted follow-up messages to the same sub-agents for
exact source URLs (no new research, avoided re-spending search budget). 2
dedup-checks. No usage-limit errors.

## Cycle 4 — 00:17-00:26 UTC

Note: this cycle's window was again short (~30 min from ingest to deadline,
00:47Z). Spent it on the mandatory watching-check, a fast discovery sweep
(cheap, 3 searches), a light triage of a very thin fresh-ingest batch (11
raw items, mostly off-scope or already-covered), and one verify+publish
pass on the one candidate that cleared scope/substance/dedup. Skipped a
fresh Tier-0 html sweep (cycle 1 already covered all 17 sources this run;
nothing in this cycle's ingest pointed at a specific Tier-0 source needing
a re-fetch).

### Watching stories

Not re-checked. Neither `openai-safety-heidecke-departure` nor
`meta-ai-chip-production` (nor its MTIA/Iris framing) appear in this
cycle's fresh ingest (`data/raw/2026-07-19/*.json`, grepped for
"Heidecke", "chip", "MTIA", "Iris" -- only unrelated Apple/Nvidia
market-cap chip items matched "chip"). Left untouched per instructions.

### Candidates considered

- **Apple market-cap overtakes Nvidia** (XenoSpectrum, MSN, finance.biggo.com,
  18 July) -- skipped: primarily a stock-market-cap story, thin AI angle
  (framed as "low capex AI monetization strategy" by one outlet only);
  fails residual-substance test as an AI-vertical story.
- **Meta sued over AI-driven layoffs** (ad-hoc-news.de, 18 July) --
  dedup-check not run (title/topic obviously matches); this is the same
  underlying lawsuit as the already-published `meta-ai-layoffs-
  discrimination-lawsuit` (14 July) -- skipped-duplicate, no new detail in
  this German-language relay beyond what's already covered.
- **Kimi K3 / Anthropic valuation** (Crypto Briefing, 18 July) --
  skipped-duplicate: same story as already-published `kimi-k3-release`.
- **Co-op "dangerous" AI delivery robots** (raw ingest headline; researched
  via web search since no direct article body in the raw item) -- a
  UK pedestrian-advocacy campaign (Living Streets) calling delivery robots
  a pavement-safety hazard, citing a 2022 Starship Technologies incident.
  Skipped: judged out of scope (§0 -- this is a physical-safety/urban-mobility
  regulatory story about delivery robots generally, not an AI capability,
  lab, research, or AI-specific-policy story; the "AI" framing is
  incidental to the pavement-safety complaint) and thin on new information
  (campaigner statement plus a four-year-old incident, not new).
- **Pro-AI super PAC Leading the Future's Q2 FEC filing ($31M cash on
  hand)** (raw ingest via streamlinefeed.co.ke relay of Axios; also
  surfaced independently in the "AI regulation law passed" discovery
  query context) -- dedup-check run: moderate match to already-published
  `anthropic-amodei-pac-donation` (16 July). Read the matched article: same
  ongoing AI-super-PAC-money saga, but this is a NEW disclosure (Q2 FEC
  filing) not covered by the original piece -- follow-up per §8, not an
  in-place update. **Published** as `leading-the-future-pac-31m-midterms`
  with `follows: anthropic-amodei-pac-donation`. See verification notes
  below.
- Remaining raw-ingest items (jail-inmate-arrest false-positive keyword
  match, no-code AI agent builder listicle, "Claude Mythos" clickbait
  video title, Lagos AI-skills op-ed, Punch Newspapers) -- all failed
  scope or residual-substance test on inspection, not pursued further.

### Verification notes — `leading-the-future-pac-31m-midterms` (published)

Fresh verifier and fresh adversarial falsifier ran in parallel (both
independent, neither saw the other's work until synthesis here). Both
fetched the primary Axios article directly via a reader-proxy (direct
fetch to axios.com 403'd for both, a repeat of the known Axios-fetch
pattern from earlier nights).

Both agents independently caught the same problem with the initial
raw-ingest framing: the specific dollar figures for individual donors
(Andreessen, Horowitz, Brockman -- reported elsewhere as "~$25M each")
do **not** appear in the 17 July Axios article being cited, and trace
instead to two earlier, separate Axios pieces (30 Jan and 16 Apr 2026)
describing the PAC's original 2025 launch funding -- and even there,
the $25M figure is a *combined* firm+co-founders Q1 tranche, not $25M
per person (best-supported per-person figure elsewhere is $12.5M each,
from a different filing period entirely). This would have been a
sourcing/staleness error if published as originally framed from the
raw-ingest headline. Cut entirely from the article rather than force a
citation under time pressure -- not load-bearing for the story anyway.

The falsifier also caught that the "$24M spent on primary races through
June" figure is not in the cited 17 July Axios piece at all (it has zero
dollar figure for primary spending) -- the verifier then traced this
figure to CNBC's independent 9 July report (predates Axios by over a
week, so not copied from it), which supplied the correct, correctly-dated
number. Reassigned the citation to CNBC (already Tier-2 registered)
instead of Axios for that specific claim.

The $31M cash-on-hand headline figure is confirmed by two independent
primary-document-sourced accounts: Axios's own citation of "its latest
FEC filing" and Transformer News's independent FEC-filing tracker for the
same committee (C00916114), which shows $31.03M cash on hand as of 30
June 2026 -- these are not the same source relaying each other. The $20M
transfer-to-two-PACs breakdown is single-source (Axios only, not
independently corroborated this cycle) and was included as attributed,
non-load-bearing detail rather than a hard-confirmed claim, per the
precedent set in `xai-grok-build-privacy-incident` (cycle 3) for
single-source detail that isn't the story's crux.

Three claims published: (1) $31M cash on hand, Q2 close -- `confirmed`,
Axios + Transformer News; (2) >$24M spent on primaries through June --
`confirmed`, CNBC; (3) $20M transferred to Think Big PAC/American Mission
PAC -- `single-source`, attributed to Axios's FEC-filing review.

style-gate: leading-the-future-pac-31m-midterms EN pass / TR pass
(headline states two falsifiable, confirmed dollar figures, no
superlatives; TL;DR stands alone; no throat-clearing/padding/empty
closers; Turkish mirrors structure and the single-source attribution
language naturally, not calque).

### Discovery sweep (queries 10-12 of 12, index 9)

1. "AI regulation law passed" -- **miss** for new candidates (evergreen
   legislation-tracker content: NCSL, White & Case, Brennan Center --
   nothing dated in-window), but useful background confirmation of
   already-known context (TAKE IT DOWN Act, DEFIANCE Act, Illinois/
   Colorado state laws already covered on this site).
2. "AI safety evaluation report published" -- **miss**: surfaced the
   International AI Safety Report 2026 (3 Feb 2026), outside this cycle's
   window and not a new item.
3. "AI incident harm report" -- **miss**: only evergreen incident-database
   resources (MIT AI Incident Tracker, AIID, CSET), nothing dated
   in-window.

Query-pool note: queries 10-12 (regulation/safety-report/incident-report)
were all misses for fresh candidates this cycle -- they lean toward
evergreen reference content rather than dated news. Given three straight
misses, worth considering more specific phrasing (e.g. adding a date
qualifier or swapping "report published" for "report this week") in a
future source-lifecycle review; not changed this cycle per the
no-policy-file-edits rule for night runs.

### Discovery logging

Two unregistered evidence domains used this cycle: `axios.com` (second
recurrence -- see also 10 July) and `elections.transformernews.ai` (new,
independent FEC-filing tracker) -- both logged in
`source_candidates.json` under `candidates_2026-07-19_night_c4`.

### Budget note

1 story verified/published this cycle (1/4 cycle cap, 3/12 night cap used
across cycles 1-4). ~5 web searches/fetches for discovery sweep and
candidate triage, plus ~9 and ~14 tool calls respectively across the
parallel verifier and falsifier sub-agents (~65k tokens combined). 1
dedup-check. No usage-limit errors.

## Cycle 5 — 00:52-01:02 UTC

Note: shortest window of the night (~26 min from ingest to the 01:18Z
deadline). Spent it on the two mandatory watching-rechecks (both quick,
site-scoped single searches, no change) and one in-place update to an
already-published story, rather than a fresh Tier-0/discovery sweep or a
new-story verify+publish pass -- a brand-new story's full multi-agent
protocol plus bilingual editor gate could not be safely finished in the
remaining time, but an in-place update (narrower scope, no new article
structure) could. Tier-0 sweep skipped (cycle 1 already covered all 17
sources tonight, nothing in this cycle's ingest pointed at a specific
source needing a re-fetch). Discovery sweep skipped: the index formula
(((5-1)*3) mod 12) lands back on index 0, i.e. the same three queries
("AI model release announcement", "open source LLM release weights",
"LLM benchmark results independent evaluation") cycle 1 already ran
earlier tonight -- re-running them for a second time this run would not
have produced new signal, so the budget went to the update pass instead.

### Watching stories re-checked (2/2)

- `openai-safety-heidecke-departure`: re-checked, 1 web search. No change --
  same outlets (Bloomberg-citing-Wired, PYMNTS, citybiz, cryptobriefing,
  techtimes, storyboard18) still trace to the single Wired-sourced Mark Chen
  memo/quote. No independent OpenAI/Heidecke contact, no title confirmation
  for Glaese/Jain, no Heidecke statement. Gate still fails on
  wire-exclusivity. Still watching; reported effective-departure date
  2026-07-24 is 5 days out.
- `meta-ai-chip-production`: re-checked, 1 web search (site-scoped to
  exclude reuters.com). No change -- same outlets (cryptobriefing, barchart,
  qz.com, letsdatascience, FullStackEvolved, threads.com, finance.yahoo.com,
  MLQ, techtimes) still repeat the same 9 July Reuters/internal-memo details
  verbatim. No Meta statement, filing, or independent second source. Gate
  still fails on independence. Eleventh consecutive night unchanged;
  TrendForce-corroborated narrower sub-story remains available to split out
  if the owner wants it, flagged again, still unactioned.

### Candidates considered

Fresh ingest this cycle (`data/raw/2026-07-19/*.json`, 6 files, 265 raw
items) was light and mostly off-scope or duplicate:

- **Apple market-cap overtakes Nvidia** (XenoSpectrum, MSN, finance.biggo.com,
  18 July, recurring from cycle 4's ingest) -- skipped again: thin AI angle,
  primarily a stock-market-cap story.
- **China unveils powerful new AI model, pushes chip stocks into bear
  market** (GuruFocus, Startup Fortune, 18 July) -- not pursued: no model
  name or specific claim in the raw items, would need dedicated research
  this cycle had no budget for; flagging for a future cycle's triage.
- **Meta AI-driven layoffs lawsuit, judge won't block firings** (MSN,
  ad-hoc-news.de, 18 July) -- skipped-duplicate/not actioned: same
  underlying case as already-published `meta-ai-layoffs-discrimination-
  lawsuit`; this specific procedural update (judge declining to block
  Meta from terminating the plaintiffs) looks like a legitimate in-place
  update candidate but this cycle's time budget went to the Apple/OpenAI
  item instead -- flagging for the next available cycle/night.
- **Google and OpenAI employees back Anthropic in legal fight over military
  AI use** (MSN, 18 July) -- dedup-check run: moderate match to
  already-published `sf-stop-the-ai-race-protest` (score 0.4, a street-protest
  story, not the same event). Likely a standalone-justified case per §8(c)
  but not pursued this cycle for time -- flagging as a candidate for a
  future cycle, not yet triaged past the dedup check.
- **EU compels Google to share Android/Search data with rival AI assistants**
  (Unite.AI, 18 July) -- not dedup-checked this cycle; very likely the same
  underlying story as already-published `eu-google-ai-android-
  interoperability` (confirmed moderate match in cycle 1's Tier-0 sweep on
  17 July). Not actioned.
- **Californians back CEQA reform, reject AI data centers in poll** (AOL.com,
  18 July) -- not pursued this cycle for time; potential new candidate,
  flagging for a future night.
- **Apple sends legal letters to dozens of OpenAI defectors** (Mashable,
  Times of India relays of a 17 July report) -- dedup-check: no match on
  title/URL similarity, but manually identified as the same underlying case
  as already-published `apple-openai-trade-secrets-lawsuit` (Apple v. Liu).
  **Published as an in-place update.** See verification notes below.
- Remaining raw-ingest items (Lagos AI-skills op-ed x2, Nigeria satellite
  agriculture, Co-op delivery robots [already assessed cycle 4], jail-inmate
  arrest and Kindle-jailbreak false-positive keyword matches, no-code AI
  agent builder listicle, Kimi K3/Anthropic-valuation and Claude-Mythos
  items [already assessed/duplicate from cycle 4]) -- all failed scope,
  residual-substance, or duplicate checks on inspection.

### Verification notes — `apple-openai-trade-secrets-lawsuit` (in-place update, published)

Fresh verifier and fresh adversarial falsifier ran independently (neither
saw the other's work until synthesis here). Both traced the "~40 former
Apple employees sent legal letters" headline (as relayed by Mashable/Times
of India) back to its origin: a paywalled Financial Times scoop citing
unnamed sources, first published 17 July 2026. Every other outlet found
(MacRumors, 9to5Mac, AppleInsider, MacDailyNews, Cybernews, Slashdot,
Business Post, Silicon Republic, plus the two ingest items) traces to that
same single FT account -- no independent second source, no court filing,
confirms the ~40 figure or the "preservation letter" characterization. The
falsifier specifically checked for a disputing account or a different
number and found none, and flagged one precision point: preservation
letters are a pre-discovery evidence-hold step (sent in anticipation of
future discovery), not discovery requests themselves -- applied to the
article's wording. Both agents confirmed OpenAI has issued no fresh comment
specific to the letters; the only available OpenAI quote is its earlier
general denial from the original lawsuit filing, already in the article.

One claim added: Apple sent preservation letters to ~40 former Apple
employees now at OpenAI, extending its evidence-preservation effort beyond
Tang Tan and Chang Liu -- `single-source` (Financial Times, via MacRumors
and 9to5Mac as the two most authoritative direct syndicators; FT itself not
linked, paywalled). Outcome (a) per §8: same event (Apple v. Liu case), new
discovery-adjacent detail, in-place update rather than a new article.

While editing, also found and fixed a pre-existing structural bug in the
TR article unrelated to this update: its two prior Update sections
("Güncelleme, 16 Temmuz" and "Güncelleme, 14 Temmuz") were in the wrong
order and wrongly interleaved with "Neden önemli", diverging from the EN
article's structure (What happened → Why it matters → Updates
chronologically). Corrected to mirror EN exactly before adding the new
Update section.

style-gate: apple-openai-trade-secrets-lawsuit EN pass / TR pass (headline
unchanged and still states only confirmed facts; TL;DR still stands alone;
no padding/throat-clearing/empty closers in the new Update section; TR
mirrors EN structure and the single-source attribution language naturally,
not calque -- structural bug fixed as part of this pass).

### Discovery sweep

Skipped this cycle -- see note above (index formula repeats cycle 1's
queries 1-3, already run earlier tonight; budget went to the update pass
instead).

### Discovery logging

Two unregistered evidence domains used this cycle: `macrumors.com` and
`9to5mac.com` -- both established, reputable Apple-beat outlets, logged in
`source_candidates.json` for a source-lifecycle review (both have now
supplied evidence across multiple nights' Apple/OpenAI-lawsuit coverage --
case building for Tier-2 registration).

### Budget note

0 new stories published, 1 story updated in-place this cycle (0/4 cycle
cap toward new stories, 3/12 night cap used across cycles 1-4 unchanged --
in-place updates don't count against the new-story cap). ~4 web searches
for watching rechecks, ~8 tool calls combined across the parallel verifier
and falsifier sub-agents, 1 dedup-check. No usage-limit errors.

## Night summary

Five stories published tonight (cycles 1-5): `aisi-open-weight-cyber-gap`
(cycle 2), `xai-grok-build-privacy-incident` (cycle 3),
`leading-the-future-pac-31m-midterms` (cycle 4, follow-up to
`anthropic-amodei-pac-donation`), and one in-place update tonight to
`apple-openai-trade-secrets-lawsuit` (cycle 5) -- plus cycle 1 spent
entirely on sweep/discovery/watching with two candidates queued rather than
publishing. 4 of tonight's 12-story night budget used; cycles consistently
ran into a hard ~25-30 minute window each (ingest-to-deadline), which meant
every cycle after the first had to choose between a Tier-0/discovery sweep
and a full verify+publish pass rather than both -- Tier-0 sweep only ran
once (cycle 1), discovery sweep ran twice (cycles 1 and 4, covering queries
1-3 and 10-12 of 12; queries 4-9 still unrun this month's cycle-index
sequence).

Coverage gaps: `meta-ai-chip-production` (eleventh consecutive watching
night) and `openai-safety-heidecke-departure` (watching since 12 July) both
remain gated on independence/wire-exclusivity with no forward motion --
both are now well past the point where nightly rechecks are finding new
information. Several candidates surfaced but were not pursued for time:
the Meta-layoffs-lawsuit judge ruling (likely in-place update), the
Anthropic-military-AI-legal-fight story (likely standalone, dedup-cleared
to moderate/distinct), the EU-Google-Android story (likely another
in-place update to `eu-google-ai-android-interoperability`), the
Californians-reject-AI-data-centers poll, and the China-AI-model
chip-stock-selloff story (no model name pinned down yet). None of these
were dedup-checked or triaged past a first read this cycle.

## For the owner

1. `meta-ai-chip-production` has now failed the independence gate for
   eleven consecutive nights on the exact same Reuters-memo-derived claim
   set. The TrendForce-corroborated narrower sub-story (chip
   generation/node/half-year timing, excluding the September date,
   7GW->14GW capacity, and Broadcom-2029 terms) has been ready to split out
   since the first night this entry opened. Worth deciding: split it out as
   a smaller confirmed story, or keep holding for the full bundled claim
   set (which may never independently corroborate)?
2. `openai-safety-heidecke-departure` has sat in watching since 12 July on
   a single Wired-sourced account with no second outlet independently
   reaching OpenAI. The claimed effective-departure date (24 July) is 5
   days out -- is it worth relaxing the wire-exclusivity bar for a
   well-attested personnel departure once the date itself passes and no
   contradicting account has surfaced, or should the entry keep waiting
   indefinitely for a second source that may never come?
3. Every cycle this run hit a ~25-30 minute ingest-to-deadline window,
   forcing a cycle-by-cycle tradeoff between Tier-0 sweep, discovery sweep,
   and verify+publish -- only one full Tier-0 sweep and two of six
   discovery-sweep slices ran across all 5 cycles tonight. Is the per-cycle
   window intentionally this tight, or would a schedule adjustment (fewer,
   longer cycles; or starting ingest earlier relative to the deadline) let
   each cycle cover more ground?
4. MacRumors and 9to5Mac have now supplied evidence across multiple nights
   of the Apple/OpenAI trade-secrets coverage (this cycle plus prior
   nights) without being formally registered in `policy/sources.yaml`.
   Worth a source-lifecycle review to promote them to Tier-2, given they're
   the most reliable direct syndicators of Apple-beat scoops that originate
   behind paywalls (Bloomberg, FT)?
5. Multiple candidates this cycle were identified but not fully triaged for
   time (Meta-layoffs judge ruling, Anthropic-military-AI legal fight,
   EU-Google-Android update, CEQA/AI-data-center poll, China-AI-model
   chip-selloff). None carry deadline pressure, but they're listed above so
   tomorrow night's cycle 1 can pick them up without re-discovering them
   from scratch.

## Loop supervisor footer

- Cycles run: 5 (successful: 5, max: 6)
- New articles: 3 · updated articles: 1 (night cap: 12 new)
- Usage-limit stop: no
- Window: 2026-07-18T22:29:40Z → 2026-07-19T00:59Z
