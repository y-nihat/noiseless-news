[pytest]
testpaths = pipeline/tests
# The package lives under pipeline/, not the repo root. Setting it here means a
# bare `pytest` works everywhere — in the container, in CI, and on a machine
# that has never exported PYTHONPATH.
pythonpath = pipeline
