services:
  pipeline:
    build:
      context: .
      dockerfile: pipeline/Dockerfile
    env_file:
      - path: .env
        required: false   # absent in CI; the token lives in Actions secrets there
    environment:
      PYTHONPATH: /app/pipeline
    volumes:
      - .:/app
    working_dir: /app
